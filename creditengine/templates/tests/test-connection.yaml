apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "creditengine.fullname" . }}-test-connection"
  labels:
    {{- include "creditengine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "creditengine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
