apiVersion: v1
kind: Service
metadata:
  name: {{ include "creditengine.fullname" . }}
  labels:
    {{- include "creditengine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "creditengine.selectorLabels" . | nindent 4 }}
