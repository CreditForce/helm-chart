apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-origin.fullname" . }}-test-connection"
  labels:
    {{- include "api-origin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-origin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
