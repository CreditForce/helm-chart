apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-origin.fullname" . }}
  labels:
    {{- include "web-origin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-origin.selectorLabels" . | nindent 4 }}
