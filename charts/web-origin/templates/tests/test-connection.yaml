apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-origin.fullname" . }}-test-connection"
  labels:
    {{- include "web-origin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-origin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
