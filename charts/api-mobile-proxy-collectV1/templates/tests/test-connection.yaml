apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-mobile-proxy-collectv1.fullname" . }}-test-connection"
  labels:
    {{- include "api-mobile-proxy-collectv1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-mobile-proxy-collectv1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
