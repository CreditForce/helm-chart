apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-subscription.fullname" . }}
  labels:
    {{- include "api-subscription.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-subscription.selectorLabels" . | nindent 4 }}
