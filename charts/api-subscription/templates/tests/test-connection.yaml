apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-subscription.fullname" . }}-test-connection"
  labels:
    {{- include "api-subscription.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-subscription.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
