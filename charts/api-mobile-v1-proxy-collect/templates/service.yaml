apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-mobile-v1-proxy-collect.fullname" . }}
  labels:
    {{- include "api-mobile-v1-proxy-collect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-mobile-v1-proxy-collect.selectorLabels" . | nindent 4 }}
