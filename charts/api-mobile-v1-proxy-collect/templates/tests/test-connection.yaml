apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-mobile-v1-proxy-collect.fullname" . }}-test-connection"
  labels:
    {{- include "api-mobile-v1-proxy-collect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-mobile-v1-proxy-collect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
