apiVersion: v1
kind: Service
metadata:
  name: {{ include "engine-notification-hub.fullname" . }}
  labels:
    {{- include "engine-notification-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "engine-notification-hub.selectorLabels" . | nindent 4 }}
