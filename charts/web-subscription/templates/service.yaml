apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-subscription.fullname" . }}
  labels:
    {{- include "web-subscription.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-subscription.selectorLabels" . | nindent 4 }}
