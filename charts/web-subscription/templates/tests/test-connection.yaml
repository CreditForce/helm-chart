apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-subscription.fullname" . }}-test-connection"
  labels:
    {{- include "web-subscription.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-subscription.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
