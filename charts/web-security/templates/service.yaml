apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-security.fullname" . }}
  labels:
    {{- include "web-security.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-security.selectorLabels" . | nindent 4 }}
