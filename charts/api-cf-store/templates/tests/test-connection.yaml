apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-cf-store.fullname" . }}-test-connection"
  labels:
    {{- include "api-cf-store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-cf-store.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
