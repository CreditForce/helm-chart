apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-cf-store.fullname" . }}
  labels:
    {{- include "api-cf-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-cf-store.selectorLabels" . | nindent 4 }}
