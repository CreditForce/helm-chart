apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-selfie.fullname" . }}
  labels:
    {{- include "web-selfie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-selfie.selectorLabels" . | nindent 4 }}
