apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-selfie.fullname" . }}-test-connection"
  labels:
    {{- include "web-selfie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-selfie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
