apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-auth.fullname" . }}
  labels:
    {{- include "api-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-auth.selectorLabels" . | nindent 4 }}
