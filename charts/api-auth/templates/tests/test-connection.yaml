apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-auth.fullname" . }}-test-connection"
  labels:
    {{- include "api-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
