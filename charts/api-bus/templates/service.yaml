apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-bus.fullname" . }}
  labels:
    {{- include "api-bus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-bus.selectorLabels" . | nindent 4 }}
